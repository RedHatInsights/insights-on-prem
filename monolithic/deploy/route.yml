apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: insights-on-prem
spec:
  port:
    targetPort: https
  tls:
    termination: passthrough
  to:
    kind: Service
    name: insights-on-prem
